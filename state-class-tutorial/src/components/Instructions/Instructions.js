import React, { Component } from 'react';

export default class Instructions extends Component {
    render() {
        return(
            <h1>click on an emoji to view the short name.</h1>
        )
    }
}